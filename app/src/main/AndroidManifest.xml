<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- "Display over other apps": grants the background-activity-launch exemption
         the periodic sync needs to open Obsidian (so Obsidian Sync refreshes the
         vault) without Tasker. Only exercised when the user enables the toggle. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <queries>
        <package android:name="com.getpebble.android.basalt" />
        <package android:name="com.getpebble.android.google" />
        <package android:name="io.rebble.store" />
        <package android:name="coredevices.coreapp" />
        <!-- Needed for getLaunchIntentForPackage on Android 11+ (package visibility). -->
        <package android:name="md.obsidian" />
        <intent>
            <action android:name="com.getpebble.action.app.RECEIVE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/octet-stream" />
        </intent>
    </queries>

    <application
        android:name=".ObsidianApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.ObsidianTasksPebbleBridge"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".BackgroundReceiver"
            android:enabled="true"
            android:exported="true">
            <!-- Pebble watch messages (com.getpebble.action.app.RECEIVE) are delivered
                 to the runtime-registered receiver in PebbleBridgeService, NOT here:
                 Android 8+ won't deliver that implicit broadcast to manifest receivers,
                 and on Android 7 a manifest filter caused EVERY watch message to be
                 handled twice (manifest + runtime). So this receiver only handles the
                 explicit SYNC_NOW trigger (e.g. Tasker after opening Obsidian). -->
            <intent-filter>
                <action android:name="com.example.obsidiantaskspebblebridge.SYNC_NOW" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Shizuku: lets the optional "invisible mode" talk to the Shizuku service.
             Harmless when Shizuku isn't installed (the binder simply never connects). -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <receiver
            android:name=".ReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".PebbleBridgeService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBCATEGORY"
                android:value="Keeps a runtime broadcast receiver alive to deliver Pebble watch actions (Android 8+ blocks implicit broadcasts to manifest receivers)." />
        </service>

    </application>

</manifest>